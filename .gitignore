# Secrets — never commit
.env
.env.*
!.env.example

# Kronagent runtime state (operational, not source)
kronagent_audit.jsonl
kronagent_approvals.json
kronagent_allowlist.json
*.jsonl
# Demo scratch state (demo_trajectory.py writes these; never real runtime data)
kronagent_trajectory_demo*.json

# Presenter notes — kept local, not part of the repo
DEMO_SCRIPT.md
*.pem
*.db

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# OS / editor
.DS_Store
*.swp
.idea/
.vscode/

# Claude Code harness state — not source
.claude/
