# Secrets — never commit
.env
.env.*
!.env.example

# Kronagent runtime state (operational, not source)
kronagent_audit.jsonl
kronagent_approvals.json
kronagent_allowlist.json
kronagent_connections.json
kronagent_trajectory_halt.json
*.jsonl
# Demo scratch state (demo_trajectory.py writes these; never real runtime data)
kronagent_trajectory_demo*.json

# Presenter notes — kept local, not part of the repo
DEMO_SCRIPT.md

# Commercial planning and candid self-assessment — kept local. This repo is
# public. Ship dates, competitive positioning, monetisation strategy and an
# itemised list of where the product is weak are useful to a competitor and to
# nobody else. kronagent_next_steps.md additionally documented open defects
# with reproduction detail, which is a vulnerability disclosure we did not
# intend to make.
kronagent_product_roadmap.md
research.md
kronagent_next_steps.md
*.pem
*.db

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# OS / editor
.DS_Store
*.swp
.idea/
.vscode/

# Claude Code harness state — not source
.claude/

# build output — the source of truth is kronagent/, not a stale copy of it
build/
dist/

# Demo temp files
.demo_tmp/

