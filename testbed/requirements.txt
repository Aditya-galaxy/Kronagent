# Kronagent testbed dependencies (local SQS emulation — no AWS account, no Docker).
# Kept separate from the app's requirements: the platform never needs these at
# runtime, only the local testbed and the SQS integration tests do.
moto[server]>=5.2
boto3>=1.43
