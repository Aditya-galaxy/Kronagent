apiVersion: v1
kind: Service
metadata:
  name: {{ include "kronagent.fullname" . }}
  labels:
    {{- include "kronagent.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "kronagent.selectorLabels" . | nindent 4 }}
